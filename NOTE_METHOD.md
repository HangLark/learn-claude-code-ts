# 数据视角章节笔记方法论

## 目标

用最少的内容说明本章新增了什么数据和逻辑，这些数据如何进入已有流程，以及什么条件驱动它们变化。

## 分析顺序

### 1. 确定本章增量

先与前一章比较，找出新增或发生变化的数据结构、处理节点和约束。s01 负责建立 Agent Loop 基线，后续章节只解释增量。

### 2. 找新增的核心数据

找出决定新增逻辑下一步行为、并会在运行过程中变化的数据。说明它的类型、存储位置和生命周期，并区分以下角色：

- 状态：系统持续维护的数据。
- 配置：提供规则但不随主流程变化的数据。
- 事件：触发一次处理的数据。
- 派生数据：由已有数据筛选或转换得到的数据。
- 外部观察：工具或环境返回的数据。

### 3. 使用权威类型解释协议数据

当新增逻辑依赖 Anthropic API 的数据结构时，优先使用本项目安装的 `@anthropic-ai/sdk` TypeScript 类型，例如 `Anthropic.MessageParam`、`Anthropic.ToolUseBlock` 和 `Anthropic.ToolResultBlockParam`。

- 类型第一次出现时必须展开结构并解释关键字段，不能只列类型名称。
- 类型较短时直接摘录 SDK 的准确声明；类型很大时使用 `Pick<...>` 明确表示只展示本章依赖的字段，不能把简化结构伪装成完整定义。
- 已在前面章节展开过的 SDK 类型，后续章节只说明新增用法，不重复定义。

### 4. 追踪新增数据流

按“生产者 → 转换 → 消费者”描述新增数据从哪里来、在哪个节点进入已有流程、最终进入哪里。优先使用一段简短流程图，不罗列函数调用。

### 5. 提炼新增的状态转换与不变量

记录本章新增的状态变化条件，以及实现必须保持的关联关系、顺序和边界。没有新增状态机时，不强行增加状态转换章节。

### 6. 检查闭环是否变化

只有当本章改变了继续、停止或重新进入循环的条件时，才解释新的闭环；否则不重复已有 Agent Loop。

## 内容取舍

- 只解释当前章节代码可以直接观察到的机制。
- 以相对上一章的增量为主，不提前引入后续章节。
- 前面章节已经解释过的内容不再赘述；必要时用一句话指出新逻辑插入已有流程的位置。
- 必要时展开 `@anthropic-ai/sdk` 的准确类型；首次出现必须让读者看见具体结构。
- 关注数据语义和关系，不写函数清单。
- 仅当错误处理、持久化或并发控制属于本章核心时才展开。
- README 已经说明的背景和运行方式，不在 Note 中重复。

## 建议结构

```md
# sXX：章节名称

一句话说明本章相对前一章增加了什么。

## 新增的核心数据

新增或变化的数据类型、关键字段、存储位置和生命周期。

## 新增数据流

新增数据如何产生、在哪个节点进入已有流程、被谁消费。

## 状态转换 / 不变量

本章新增的转换条件、关联关系、顺序和边界。

## 闭环变化

仅在继续或结束条件发生变化时说明。
```

结构服务于内容；某一节没有新增的关键内容时应省略，而不是为了统一格式重复前章或强行填充。s01 作为基线，可以完整说明核心状态、关键数据、数据流和不变量。
